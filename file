t  GNU nano 6.2                                                                                                                      gg *                                                                                                                             
sudo apt update
sudo apt upgrade -y

  #change dir to 
sudo docker pull ubuntu
sudo docker run -i -t ubuntu
/#  apt-get update
/#  apt-get install net-tools
/#  ifconfig
/#  apt-get install asterisk -y
/#  exit

sudo docker commit {______} ubuntu
sudo docker run -i -t ubuntu
/#  apt-get install nano

cd /etc/asterisk/
mv sip.conf sip.conf.backup
mv extensions.conf extensions.conf.backup
cd 
    /#  nano /etc/asterisk/extensions.conf
    /# nano /etc/asterisk/sip.conf
          insert sip.conf and extensions.conf                                    
    /# /etc/init.d/asterisk stop
    /# /etc/init.d/asterisk start
    /# asterisk -t

sip show peers
exit
exit

 sudo docker run -d -i -t -p 5060:5060 ubuntu

sudo docker exec -i -t {_________________} sh

# 





